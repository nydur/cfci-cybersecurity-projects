name: "Bash script for Project 1"

on:
  push:
    branches:
      - main

jobs:
  run-script-job:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - name: Run a bash script
      run: bash ./project_1/sysinfo.sh
